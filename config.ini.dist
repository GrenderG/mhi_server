[server]
; Data directory containing recreated/ and original_dumps/ subdirectories.
data_directory = ./data

; Host to bind to (0.0.0.0 for all interfaces).
host = 127.0.0.1

; Port to listen on.
port = 80